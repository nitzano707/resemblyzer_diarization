services:
  - type: web
    name: fastapi-app
    env: python
    region: oregon  # או המיקום הרצוי
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app:app"
    plan: free  # או paid אם אתה משתמש בשירות בתשלום
